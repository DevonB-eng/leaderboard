import 'package:flutter/material.dart';

/*
custom_colors.dart - the custom colors for the app
- file for making my app look pretty. more to come
*/

class CustomColors { // currently none of these colors/names are what I want. I need to do some branding (create a logo & color pallete)
  static final Color firebaseNavy = Color(0xFF2C384A);
  static final Color firebaseOrange = Color(0xFFF57C00);
  static final Color firebaseYellow = Color(0xFFFFA000);
  static final Color firebaseGrey = Color(0xFFECEFF1);
  static final Color googleBackground = Color(0xFF4285F4);
}
